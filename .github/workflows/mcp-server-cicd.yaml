name: m8flow-mcp-server CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "m8flow-mcp/**"
      - ".github/workflows/mcp-server-cicd.yaml"

  pull_request:
    branches:
      - main
    paths:
      - "m8flow-mcp/**"
      - ".github/workflows/mcp-server-cicd.yaml"

  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to promote/deploy to"
        required: true
        default: "demo"
        type: choice
        options:
          - demo
          - qa
      image_uri:
        description: "Pre-built Image URI to promote (e.g. m8flow/m8flow-mcp-server-service:dev-a1b2c3d or release tag)"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: mcp-server-cicd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  ci:
    name: MCP Server CI Pipeline
    if: github.event_name != 'workflow_dispatch'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/ci-orchestrator.yaml@d74dfcd7c92886f2b74dbaf98536e6bde6d0be99
    with:
      config_path: m8flow-mcp/devx-ci.yaml
      auto_merge: false
      push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    secrets:
      registry_username: ${{ secrets.DOCKERHUB_USERNAME }}
      registry_password: ${{ secrets.DOCKERHUB_TOKEN }}
      sonar_token: ${{ secrets.SONAR_TOKEN }}

  cd-dev:
    name: Deploy MCP Server → DEV
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@d74dfcd7c92886f2b74dbaf98536e6bde6d0be99
    with:
      environment: dev
      image_uri: ${{ needs.ci.outputs.image_uri }}
      config_path: m8flow-mcp/devx-config.yaml
    secrets: inherit

  cd-demo:
    name: Deploy MCP Server → DEMO
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'demo'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@d74dfcd7c92886f2b74dbaf98536e6bde6d0be99
    with:
      environment: demo
      image_uri: ${{ github.event.inputs.image_uri }}
      config_path: m8flow-mcp/devx-config.yaml
    secrets: inherit

  cd-qa:
    name: Deploy MCP Server → QA
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'qa'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@d74dfcd7c92886f2b74dbaf98536e6bde6d0be99
    with:
      environment: qa
      image_uri: ${{ github.event.inputs.image_uri }}
      config_path: m8flow-mcp/devx-config.yaml
    secrets: inherit
